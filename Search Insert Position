class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        
        int result = 0;
        for(auto num : nums)
            if(num >= target)
                break;
            else
                result++;
        
        return result;
    }
};
